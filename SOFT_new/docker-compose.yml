services:
  s2:
    build:
      context: .
      dockerfile: Dockerfile
    image: s2-process-v3
    container_name: s2_process_v3
    env_file:
      - .env
    volumes:
      - ./configs:/workspace/configs
      - ./input:/workspace/input
      - ./output:/workspace/output
      - ./src:/workspace/src
    working_dir: /workspace
    command: ["/workspace/configs/pipeline.json"]
